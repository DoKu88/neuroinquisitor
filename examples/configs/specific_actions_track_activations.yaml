name: Track Activations
description: Captures intermediate module activations at a saved checkpoint using ReplaySession — raw, mean, and pooled reduction modes.
tags:
  - specific-action
  - replay
  - activations

# training
num_train_epochs: 5
train_lr: 0.05
n_train: 64

# replay
n_eval: 16
eval_batch_size: 8
replay_checkpoint: 4
