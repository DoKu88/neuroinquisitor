name: Snapshot Selection
description: Demonstrates SnapshotCollection selection patterns — all epochs, early/late splits, single-layer, and combined selects — with animated GIF output.
tags:
  - specific-action
  - snapshot-selection
  - visualization
dependencies:
  - matplotlib

# data
n_samples: 128

# training
num_epochs: 40
lr: 0.01

# visualization
gif_fps: 4
