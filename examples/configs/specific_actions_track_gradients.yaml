name: Track Gradients
description: Captures output gradients at a saved checkpoint using ReplaySession — per-example and aggregated gradient modes for sensitivity analysis.
tags:
  - specific-action
  - replay
  - gradients

# training
num_train_epochs: 5
train_lr: 0.05
n_train: 64

# replay
n_eval: 16
eval_batch_size: 8
replay_checkpoint: 4
