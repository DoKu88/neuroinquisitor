name: Replay Logits
description: Captures raw model output (logits) at a saved checkpoint using ReplaySession with capture=["logits"].
tags:
  - specific-action
  - replay
  - logits

# training
num_train_epochs: 5
train_lr: 0.05
n_train: 64

# replay
n_eval: 16
eval_batch_size: 8
replay_checkpoint: 4
